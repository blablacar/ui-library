import React from 'react'

import { StyledMessagingSummaryItem } from './MessagingSummaryItem.style'

export type MessagingSummaryItemProps = Readonly<{}>

export const MessagingSummaryItem = ({}: MessagingSummaryItemProps) => {
  return <StyledMessagingSummaryItem> Content </StyledMessagingSummaryItem>
}
