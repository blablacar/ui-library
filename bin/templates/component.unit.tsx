import React from 'react'

import ${componentName} from '${name}'

it('Should do something', () => {
  expect('This').toBe('To be working')
})

it('Should have the accessibility attributes', () => {

})
